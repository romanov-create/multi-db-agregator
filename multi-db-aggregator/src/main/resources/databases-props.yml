databases:
  - name: postgres1
    url: jdbc:postgresql://postgres-host:5432/postgres1
    username: postgres
    password: postgres
    driverClassName: org.postgresql.Driver
    mapping:
      table: users
      id: user_id
      username: login
      name: first_name
      surname: last_name

  - name: postgres2
    url: jdbc:postgresql://postgres-host:5432/postgres2
    username: postgres
    password: postgres
    driverClassName: org.postgresql.Driver
    mapping:
      table: user_table
      id: ldap_id
      username: ldap_login
      name: ldap_name
      surname: ldap_surname